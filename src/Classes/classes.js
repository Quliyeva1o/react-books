export default class Films {
    constructor(name, author, img, price, review) {
        this.name = name,
            this.author = author,
            this.img = img,
            this.price = price,
            this.review = review
    }
}